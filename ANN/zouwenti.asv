clear all;clc; 
  
 X = [0:0.1:1;1:0.1:2]; 
 T=[2:0.2:4;5:0.2:7];
%  T=sin(5*X)+X;
%   
 net = newrb(X,T); 
 x1 = [0.1:0.1:1.1]; 
 Y1 = sim(net,x1); 
 %T2 = sim(net,X); 
  
 figure(1); 
 plot(X,T,'b*',x1,Y1,'r+'); 


%得出一个问题：rbf神经网络训练后。当测试数据超出训练数据的大小范围时，网络输出成了固定值，而且不能随着测试数据的增大而增大，为什么？？？？？
%说明rbf只能用来聚类，不能用来预测！！！
%而bp网络貌似可以用来预测

%bp网络也不能用来预测，超出测试样本空间范围的数！！！